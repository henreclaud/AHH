# Installed dependencies (re-created by "npm install")
node_modules/

# SQLite — no longer used, but ignore just in case
database.sqlite

# Mac system files
.DS_Store

# Local environment variables (contains secrets — never commit)
.env

# Service account credentials if saved as a file
*.credentials.json
service-account*.json
